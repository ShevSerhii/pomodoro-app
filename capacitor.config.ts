import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pomodoro-app',
  webDir: 'dist/pomodoro-app/browser',
};

export default config;
